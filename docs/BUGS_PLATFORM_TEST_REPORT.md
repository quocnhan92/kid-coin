# Platform Infrastructure — Test Report

> Generated by `/tester` — 2026-06-05

## Bugs found & fixed (backend)

| ID | Severity | Mô tả | Fix |
|----|----------|-------|-----|
| **BUG-FF-01** | Critical | `feature_flags.key` là PK duy nhất → không lưu được override `market`/`family` cùng key | Đổi PK → `id`, unique `(key, scope, scope_value)` |
| **BUG-FF-02** | High | `scope_value=NULL` phá unique constraint PostgreSQL (nhiều row global trùng) | Normalize global → `scope_value=""` |
| **BUG-DB-03** | Medium | Model dùng `JSONB` thuần → SQLite test/CI không compile | `JsonDocument` variant (`app/core/db_types.py`) |
| **BUG-DB-04** | Medium | `domain_events_outbox.id` BigInteger không autoincrement trên SQLite | Đổi → `Integer` autoincrement |

## Open items (frontend / ops)

| ID | Owner | Mô tả |
|----|-------|-------|
| **BUG-FE-01** | frontend | `game_hub_loader.js` ẩn static grid khi API trả data — cần verify UI khi `/public-games` lỗi (fallback static) |
| **BUG-FE-02** | frontend | Kid dashboard `fetchPlayWallet` im lặng khi 401 — nên ẩn chip thay vì để hidden |
| **BUG-OPS-01** | ops | Chạy `alembic upgrade head` trước deploy; integration tests skip nếu thiếu migration 017 |

## Test files

| File | Scope | DB |
|------|-------|-----|
| `tests/test_platform_unit.py` | H3, H6 pure | None |
| `tests/test_platform_infrastructure.py` | H1, H2, H5 services | SQLite subset |
| `tests/test_platform_integration.py` | API H1, H4, H6 | PostgreSQL |

## Chạy test

```bash
./venv/bin/pytest tests/test_platform_unit.py tests/test_platform_infrastructure.py -v
./venv/bin/pytest tests/test_platform_integration.py -v -m integration  # cần PG + migration
```

## Edge cases covered

- Coin: insufficient balance, zero/negative amount
- Flags: unknown key, global off, market/family override, multi-scope same key
- Events: emit pending, process → published
- API compat: old client 426, missing header OK
- Tenant: cross-family 403
- Gating: reward playground 404 when flag off
